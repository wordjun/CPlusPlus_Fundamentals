#include<iostream>
#include<cmath>
#include<cstring>
using namespace std;

int StoI(char* str) {
	int len = strlen(str);
	int num = 0;

	for (int i = 0; i < len; i++) {
		if(str[i]<'0' || str[i] > '9')
	}
}